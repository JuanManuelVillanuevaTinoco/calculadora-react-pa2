generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Operacion {
  id        Int      @id @default(autoincrement())
  expresion String
  resultado String
  fecha     DateTime @default(now())
}